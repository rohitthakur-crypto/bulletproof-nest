/** Default HTTP handler timeout (30 seconds). */
export const DEFAULT_REQUEST_TIMEOUT_MS = 30_000;

/** Inclusive lower bound for successful HTTP status codes. */
export const HTTP_STATUS_OK_MIN = 200;

/** Exclusive upper bound for successful HTTP status codes. */
export const HTTP_STATUS_OK_MAX_EXCLUSIVE = 300;
